#
main(){
	
	int numero;
	print("");
	list lista;
	lista<-{2,4};
	lista.add(3);
	lista.add(12);
	
	for (i<-0 to lista.size("")-1){
	
		print(lista.get(i));
	}

}
#

int function fb(in int numer){int rt;}
{
	if(numer=0){
		rt<-0;
	}else{
	
		if(numer=1){
			rt<-1;
		}
		else{
			rt<-fb(numer-1)+fb(numer-2);
		}
	}
	
	return rt;
}

main(){

	print(fb(8));
}